import { fetchMyCredentials } from 'apps/ssi-platform/shared/Api/CredentialsApi'
import React, { useEffect } from 'react'

const ListCredentialsPage = ({ credentials }) => {
  
  console.log(credentials)
  return <div>test</div>
}

export default ListCredentialsPage
